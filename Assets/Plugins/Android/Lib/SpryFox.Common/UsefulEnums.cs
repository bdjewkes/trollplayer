namespace SpryFox.Common {
    
    public enum Result {
        Success,
        Failure,
    }

    public enum SidewaysDirection {
        Left,
        Right,
    }

    public enum Direction1D {
        Forwards,
        Backwards,
    }
}